import React from 'react';

export default class NoteCanvas extends React.PureComponent {
  render() {
    return (
      <div className="note-canvas">
        Three blind cats
      </div>
    );
  }
}